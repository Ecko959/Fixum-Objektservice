---
import Base from "../layouts/Base.astro";
import AreaServed from "./AreaServed.astro";
import CtaBand from "./CtaBand.astro";
import FaqAccordion from "./FaqAccordion.astro";
import Icon from "./Icon.astro";
import PageHero from "./PageHero.astro";
import RelatedServices from "./RelatedServices.astro";
import { canonicalPath, serviceSchema } from "../data/seo";

interface Props {
  title: string;
  metaTitle: string;
  description: string;
  eyebrow: string;
  heroText: string;
  image?: string;
  imageAlt?: string;
  lead: string;
  included: string[];
  /** Ablauf der Leistung als [Titel, Text]. Vorgesehen sind vier Schritte:
      Anfrage, Aufnahme mit Festpreis, Ausführung, Übergabe. */
  process: Array<[string, string]>;
  /** Einleitung über dem Ablauf, falls die Standardformulierung nicht passt. */
  processLead?: string;
  noteTitle?: string;
  noteText?: string;
  /** Kurzer, sichtbarer Vertrauensblock rechts neben dem Leistungsumfang. */
  facts?: Array<[string, string]>;
  /** Seiteneigene Fragen - erzeugen zusätzlich ein FAQ-Rich-Result. */
  faq?: Array<{ question: string; answer: string }>;
  /** Label für die Brotkrumennavigation, sonst wird eyebrow genutzt. */
  crumb?: string;
  serviceType?: string;
  areaTitle?: string;
  current: string;
}

const {
  title,
  metaTitle,
  description,
  eyebrow,
  heroText,
  image = "/images/hero-renovation.png",
  imageAlt = "",
  lead,
  included,
  process,
  processLead,
  noteTitle = "Klarer Umfang statt Ratespiel",
  noteText = "Nach Besichtigung oder aussagekräftigen Fotos bekommen Sie ein Angebot mit Leistungsumfang, Termin und Preis. Was nicht Teil der Leistung ist, steht genauso klar drin.",
  facts = [
    ["Besichtigung", "Kostenlos und unverbindlich, meist in zwei Werktagen."],
    ["Angebot", "Festpreis in Textform, mit klarer Leistungsgrenze."],
    ["Abwicklung", "Ein Ansprechpartner vom Anruf bis zur Übergabe."],
  ],
  faq = [],
  crumb,
  serviceType,
  areaTitle,
  current,
} = Astro.props;

const path = canonicalPath(current);
const breadcrumbs: Array<[string, string]> = [
  ["Leistungen", "/leistungen/"],
  [crumb ?? eyebrow, path],
];

const schema = [serviceSchema({ name: title, description, path, serviceType })];
---

<Base
  title={metaTitle}
  description={description}
  canonical={path}
  breadcrumbs={breadcrumbs}
  schema={schema}
  faqSchema={false}
  preloadImage={image}
  pageType="WebPage"
>
  <PageHero
    eyebrow={eyebrow}
    title={title}
    text={heroText}
    image={image}
    imageAlt={imageAlt}
    breadcrumbs={breadcrumbs}
  />

  {/* Leistungsumfang */}
  <section class="section">
    <div class="container split split--top">
      <div class="stack" data-reveal>
        <p class="eyebrow">Leistungsumfang</p>
        <h2>Was dazugehört</h2>
        <p class="lead">{lead}</p>

        <ul class="check-list detail-check">
          {included.map((item) => <li>{item}</li>)}
        </ul>
      </div>

      <aside class="facts card" data-reveal aria-label="Ablauf auf einen Blick">
        <p class="facts-title">So läuft es ab</p>
        <dl>
          {
            facts.map(([term, text]) => (
              <div class="fact">
                <dt>{term}</dt>
                <dd>{text}</dd>
              </div>
            ))
          }
        </dl>
        <a class="arrow-link" href="/kontakt/">Festpreis anfragen</a>
      </aside>
    </div>
  </section>

  {
    /* Ablauf - auf jeder Leistungsseite derselbe Bogen von der Anfrage
      bis zur Übergabe, inhaltlich aber auf die Leistung gemünzt. */
  }
  <section class="section section--mist" aria-labelledby="ablauf-title">
    <div class="container section-head" data-reveal>
      <p class="eyebrow">Ablauf</p>
      <h2 id="ablauf-title">Von der Anfrage bis zur Übergabe</h2>
      <p class="muted">
        {
          processLead ??
            "Jeder Schritt ist abgeschlossen, bevor der nächste beginnt - und Sie wissen vorher, was er kostet."
        }
      </p>
    </div>

    <ol class="container process-flow" data-reveal-group>
      {
        process.map(([headline, text]) => (
          <li class="flow-step" data-reveal>
            <span class="flow-no" aria-hidden="true" />
            <h3>{headline}</h3>
            <p>{text}</p>
          </li>
        ))
      }
    </ol>

    <p class="container process-note">
      Kein Vertreterbesuch, kein Abschlussdruck, keine Unterschrift vor Ort
      nötig.
    </p>
  </section>

  {/* Leistungsgrenzen */}
  <section class="section section--tight">
    <div class="container note-card card card--accent" data-reveal>
      <span class="note-icon"><Icon name="shield" size={24} /></span>
      <div>
        <p class="eyebrow">Wichtig zu wissen</p>
        <h2>{noteTitle}</h2>
        <p class="muted">{noteText}</p>
      </div>
    </div>
  </section>

  {
    faq.length > 0 && (
      <section class="section section--soft">
        <div class="container section-head" data-reveal>
          <p class="eyebrow">Häufige Fragen</p>
          <h2>Was Auftraggeber vorher wissen wollen</h2>
        </div>
        <div class="container container--narrow faq-wrap" data-reveal>
          <FaqAccordion items={faq} />
        </div>
      </section>
    )
  }

  <AreaServed service={crumb ?? eyebrow} title={areaTitle} />
  <RelatedServices current={current} />
  <CtaBand />
</Base>

{
  /* FAQ-Rich-Result nur, wenn die Fragen tatsächlich auf der Seite stehen. */
  faq.length > 0 && (
    <script
      is:inline
      type="application/ld+json"
      set:html={JSON.stringify({
        "@context": "https://schema.org",
        "@type": "FAQPage",
        mainEntity: faq.map((item) => ({
          "@type": "Question",
          name: item.question,
          acceptedAnswer: { "@type": "Answer", text: item.answer },
        })),
      })}
    />
  )
}

<style>
  .detail-check {
    padding-left: clamp(1.5rem, 3vw, 2rem);
    padding-right: clamp(1.5rem, 3vw, 2rem);
    border: 1px solid var(--line);
    border-radius: var(--radius-md);
    background: var(--mist-soft);
  }

  /* ---- Faktenkarte ---- */
  .facts {
    position: sticky;
    top: calc(var(--header-h) + 24px);
    padding: clamp(1.5rem, 3vw, 2rem);
  }

  .facts-title {
    margin-bottom: 1.125rem;
    font-family: var(--font-utility);
    font-size: 0.875rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--blue);
  }

  .facts dl {
    display: grid;
    gap: 1rem;
    margin: 0 0 1.25rem;
  }

  .fact {
    display: grid;
    gap: 0.2rem;
    padding-left: 1rem;
    border-left: 2px solid var(--line);
  }

  .fact dt {
    font-weight: 800;
    font-size: 1rem;
    color: var(--navy);
  }

  .fact dd {
    margin: 0;
    font-size: 0.9375rem;
    line-height: 1.55;
    color: var(--muted);
  }

  /* Ablauf: .process-flow steht im Designsystem (global.css), damit alle
     Seiten mit Ablauf dieselbe Darstellung benutzen. */

  /* ---- Hinweisblock ---- */
  .note-card {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 1.25rem;
    padding: clamp(1.5rem, 3vw, 2.25rem);
    background: var(--mist-soft);
  }

  .note-icon {
    display: grid;
    place-items: center;
    width: 52px;
    height: 52px;
    border-radius: var(--radius-sm);
    color: var(--navy-deep);
    background: var(--amber);
  }

  .note-card h2 {
    margin: 0.35rem 0 0.75rem;
    font-size: var(--step-3);
  }

  @media (max-width: 900px) {
    .facts {
      position: static;
    }

    .note-card {
      grid-template-columns: 1fr;
    }
  }
</style>
