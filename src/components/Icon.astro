---
/**
 * Inline-SVG-Icons. Bewusst ohne Icon-Font oder externes Sprite:
 * kein zusätzlicher Request, volle Kontrolle über Strichstärke und Farbe.
 */
import { iconPaths, type IconName } from "../data/icons";

interface Props {
  name: IconName;
  size?: number;
  class?: string;
  /** Nur setzen, wenn das Icon allein steht und Bedeutung trägt. */
  label?: string;
}

const { name, size = 22, class: className = "", label } = Astro.props;
const d = iconPaths[name];
---

<svg
  class={`icon ${className}`}
  width={size}
  height={size}
  viewBox="0 0 24 24"
  fill="none"
  stroke="currentColor"
  stroke-width="1.8"
  stroke-linecap="round"
  stroke-linejoin="round"
  aria-hidden={label ? undefined : "true"}
  role={label ? "img" : undefined}
  focusable="false"
>
  {label && <title>{label}</title>}
  <path d={d}></path>
</svg>

<style>
  .icon {
    flex: none;
  }
</style>
