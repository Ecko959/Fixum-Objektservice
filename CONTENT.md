# Offene Inhalte und Pflegepunkte

## Noch echte Angaben eintragen

- `src/data/site.ts`: Steuernummer ergänzen, sobald sie vorliegt.
- `src/data/site.ts`: `googleProfile` ergänzen, sobald das Unternehmensprofil online ist.
- `.env`: `PUBLIC_FORM_ENDPOINT` setzen, wenn Formspree aktiv ist.

## Fotos

- Aktuell verwendet die Seite vier mit Imagegen erzeugte realistische Website-Bilder.
- Das Vorher/Nachher-Bild aus der Anzeige liegt unter `public/images/reference-before-after.png` und ist aktiv.
- Neue echte Baustellenfotos können in `public/images/` ergänzt werden.

## Rechtliches

- AGB, Datenschutzerklärung und Widerrufsbelehrung sind als Original-PDFs eingebunden.
- Impressum enthält die bekannten Stammdaten. Die Steuernummer ist noch nicht vorhanden und wird als "wird nachgetragen" angezeigt.

## Kundenstimmen

- `src/data/testimonials.ts` ist bewusst leer.
- Keine Bewertungen eintragen, solange keine echten freigegebenen Kundenstimmen vorliegen.
